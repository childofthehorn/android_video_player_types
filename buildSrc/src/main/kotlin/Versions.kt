object Versions {
    // App
    const val appVersion = "1.0"
    const val appName = "com.stacydevino.videoplayertypes"
    const val dependencyUpdate = "0.38.0"

    object UseLibs {
        const val testRunner ="android.test.runner"
        const val testBase = "android.test.base"
        const val testMock = "android.test.mock"
    }

    // Kotlin
    const val kotlin = "1.4.30"
    const val kotlinCoroutines = "1.4.3"
    const val dokka = "1.4.32"

    // Gradle
    const val androidTools = "4.2.0-beta06"
    const val buildToolsVersion = "30.0.2"

    // Android
    const val material = "1.3.0"
    const val exoplayer = "2.13.3"

    // AndroidX
    object AndroidX {
        const val activity = "1.2.2"
        const val annotation = "1.1.0"
        const val supportLibs = "1.0.0"
        const val ktxCore = "1.3.2"
        const val appcompat = "1.2.0"

        object UI {
            const val constraintLayout = "2.0.4"
        }

        object  Test {
        const val test = "1.3.0"
        const val archTest = "2.1.0"
        const val junitExt = "1.1.2"
        const val testRules = "1.3.0"
        const val espresso = "3.3.0"
        }
    }

    // Firebase
    const val firebasePlatform = "27.1.0"
    const val firebasePerf = "1.3.5"


    object DevTools {
        const val leakCanary = "2.7"
    }

    object Test {
        const val junit = "4.13.2"
        const val supportTestRunner = "1.1.0"
        const val robolectric = "4.5.1"
        const val mockk = "1.11.0"
        const val hamcrest = "2.2"
    }
}
